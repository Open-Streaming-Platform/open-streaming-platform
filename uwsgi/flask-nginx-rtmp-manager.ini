[uwsgi]
chmod-socket = 777
chown-socket = www-data
chdir = /opt/flask-nginx-rtmp-manager
socket    = /var/uwsgi/app.sock
plugin    = python
wsgi-file = /opt/flask-nginx-rtmp-manager/app.py
process   = 3
callable = app
uid = www-data
